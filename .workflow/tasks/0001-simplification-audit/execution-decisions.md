# Execution Decisions

Last Edited: 2026-08-25

Materiality: include a choice only when a reasonable alternative could change correctness, architecture, contract, acceptance, authority, risk, reversibility, or future maintenance. Exclude mechanical edits and routine local coding choices.

| id | when | category | trigger | decision | alternatives | evidence / rationale | confidence | impact / reversibility | plan relationship | review disposition / provenance |
|---|---|---|---|---|---|---|---|---|---|---|
| D1 | Gate 3 / Phases 2, 5, and 8 / 2026-08-25 | plan deviation | The planned jsdom/testing-library and fake-indexeddb packages were not installed, and registry installation stalled or was rejected by the restricted environment. | Prove request generations and class-keyed draw decisions through extracted pure reducers/policies in Vitest; prove the rendered mounted-class flow and the real Dexie v2-to-v3 upgrade in Chromium Playwright. | Stop without proof; vendor third-party test packages; weaken the affected acceptance checks. | The alternate proof keeps deterministic state assertions in unit tests and exercises the actual browser DOM and IndexedDB boundaries. `History.test.ts`, `sessionDraw.test.ts`, `session-class-switch.spec.ts`, and `data-migration.spec.ts` all pass. | high | No production behavior compromise. Reversible by adding jsdom/fake-indexeddb tests later if those dependencies become available. | deviates only in proof mechanism; acceptance behavior is unchanged | pending — implementation record 2026-08-25 |
| D2 | Gate 3 / final E2E / 2026-08-25 | plan deviation | Playwright reported passing tests but retained the Vite web-server process, reproducing the baseline clean-exit failure. | Make `npm run e2e` use `e2e/run.mjs`, which owns the Vite server and Playwright child and closes the server after the child exits; remove Playwright's leaky `webServer` block. | Accept printed passes without exit; keep terminating the PTY manually; rely on a separately managed server. | The focused 3-test run and final 4-test run both returned exit code 0 without intervention. | high | Small runner/config expansion; reversible by restoring Playwright `webServer` if its Windows teardown becomes reliable. | extends the planned AC5 runner/shutdown fix | pending — implementation record 2026-08-25 |
